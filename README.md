# Minimal Naja sandbox

Install:

```bash
$ composer install
$ yarn install
```

And run (preferrably in two terminals, those processes run until you kill them):

```bash
$ yarn serve
$ composer serve
```
